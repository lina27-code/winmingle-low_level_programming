#include "main.h"
/**
* This function adds two integers and returns the result
* @y: the first integer
* @z: the second integer
*
* Return: (0)
* 
*/
int add(int y, int z)
{
       int result = y + z;

       return (result); 
}
